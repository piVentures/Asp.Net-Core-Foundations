# Routing & Model Binding Demo

A minimal ASP.NET Core project to learn **routing** and **model binding**.

## What this project shows
- How routes map URLs to controller actions
- How data is bound from:
  - Route parameters
  - Query strings
  - Request body (JSON)
- How `[ApiController]` simplifies validation and binding

